APP_NAME=agentic-rag-demo
APP_ENV=local
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_RETENTION_DAYS=180
LOG_MAX_FILE_SIZE_MB=20

DOCS_DIR=docs
UPLOAD_MAX_MB=20
ALLOWED_DOCUMENT_EXTENSIONS=txt,md,pdf

CHUNK_SIZE=800
CHUNK_OVERLAP=100

# OpenAI-compatible chat API.
# Qwen:     https://dashscope.aliyuncs.com/compatible-mode/v1
# DeepSeek: https://api.deepseek.com
# OpenAI:   https://api.openai.com/v1
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL_NAME=gpt-4o-mini
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=1024

# If EMBEDDING_BASE_URL or EMBEDDING_API_KEY is empty, the app falls back to LLM_BASE_URL/LLM_API_KEY.
EMBEDDING_PROVIDER=openai_compatible
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL_NAME=text-embedding-3-small
EMBEDDING_DIMENSION=1536
EMBEDDING_BATCH_SIZE=64

# Rerank provider: none | siliconflow | jina | bge
# SiliconFlow (硅基流动): https://api.siliconflow.cn/v1/rerank
# Jina AI:                      https://api.jina.ai/v1/rerank
# bge: local model placeholder (not implemented)
RERANK_PROVIDER=none
RERANK_MODEL_NAME=BAAI/bge-reranker-v2-m3
# If RERANK_BASE_URL is empty, the app picks the default URL for the chosen provider.
RERANK_BASE_URL=
# If RERANK_API_KEY is empty, the app falls back to LLM_API_KEY.
RERANK_API_KEY=
# RERANK_TOP_N: how many results to keep after reranking (N in M→N strategy).
RERANK_TOP_N=5
# RERANK_RETRIEVAL_K: how many candidates to fetch from vector store before reranking (M in M→N).
RERANK_RETRIEVAL_K=20

MILVUS_URI=http://localhost:19530
MILVUS_USER=
MILVUS_PASSWORD=
MILVUS_NO_GRPC_PROXY=
MILVUS_COLLECTION_NAME=agentic_rag_chunks
MILVUS_METRIC_TYPE=COSINE
MILVUS_INDEX_TYPE=AUTOINDEX
MILVUS_CONSISTENCY_LEVEL=Strong

RAG_TOP_K=5
RAG_SCORE_THRESHOLD=0.2
QUERY_MAX_CHARS=2000
# Retrieval method: dense | hybrid
# dense  — pure vector similarity search (Milvus COSINE)
# hybrid — dense vector + BM25 keyword, fused via Reciprocal Rank Fusion
RETRIEVAL_METHOD=dense
# Query rewrite method: none | simple | multi
# simple — LLM rewrites the question into a single retrieval-friendly query
# multi  — LLM generates multiple query variants, retrieves for each, then merges
QUERY_REWRITE_METHOD=none
# Number of query variants to generate when method=multi
QUERY_REWRITE_MULTI_COUNT=3

# Redis / short-term memory
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=
REDIS_SESSION_TTL=3600
REDIS_SESSION_PREFIX=mem:session

# Memory Gatekeeper / structured memory entries
GATEKEEPER_ENABLED=false
REDIS_ENTRY_TTL=2592000
REDIS_ENTRY_PREFIX=mem:entry
